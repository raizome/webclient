## api directory

This directory contains all the code, which is suppose to interact with the
backend API's. Any kind of code that fetches data from the backend belongs
here

The other parts of the fronend can then just call the code here and not worry
about how data is being fetched
